services:
  meme-collector:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_PATH: /data/meme_collector.sqlite3
    volumes:
      - meme_collector_data:/data
    restart: unless-stopped

volumes:
  meme_collector_data:
